services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: realchat
      POSTGRES_PASSWORD: realchat
      POSTGRES_DB: realchat
    ports:
      - "5433:5432"
    volumes:
      - realchat_pgdata:/var/lib/postgresql/data

volumes:
  realchat_pgdata:
